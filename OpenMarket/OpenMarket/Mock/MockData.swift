import UIKit

struct MockData {
    var mockGETItem = NSDataAsset(name: "getItem")
    var mockGETItems = NSDataAsset(name: "getItems")
    var mockPOSTItem = NSDataAsset(name: "postItem")
    var mockPATCHItem = NSDataAsset(name: "patchItem")
    var mockURL = URL(string: "https://www.mock.com/")!
}
